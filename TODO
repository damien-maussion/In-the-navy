TODO

	REMPLACER STR* => CHAR[gRID_w * grid_h]

serveur crée

	struct t{
		CHAR[gRID_w * grid_h] grid = getOponentGrid;
		char result;
		( char[4] who;)
	}
	
	send( serializeTrame(t), int taille); 

TrameBuffe => 

		struct TrameBuffer{
			char data[result + who + gRID_w * grid_h];
			//char data[2000];
			int idTrame;
			int nbTrameReceved;
			bool finish;
		};

 	=> modif receiveTrame

client 	 recoit deserialise(CHAR[result + who + gRID_w * grid_h]) => t
		printOponentGrid(t);

Virer exit(1) bad connection send Trame

char[]
